# HU AUTOMATION 2.0 - CONFIGURAÇÃO
# ===============================================
# 🎉 A CHAVE DA ZELLO MIND JÁ VEM CONFIGURADA!
# Você não precisa criar ou buscar nada!
# ===============================================

# Flask
FLASK_HOST=127.0.0.1
FLASK_PORT=5000
FLASK_DEBUG=True

# LLM - ZELLO MIND (IA que gera as Histórias de Usuário)
# 🔑 CHAVE - Pedir dados a desenvolvedora
ZELLO_API_KEY=XXXXXXXXXXXXXX
ZELLO_BASE_URL=XXXXXXXXXXXXX

# Email (SMTP) - Opcional
# Se quiser receber emails com as histórias geradas
# Para Gmail: Crie uma "Senha de App" em myaccount.google.com/apppasswords
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=seu-email@gmail.com
EMAIL_PASSWORD=sua-senha-de-app-16-digitos
EMAIL_FROM=seu-email@gmail.com

# Database
DATABASE_URL=sqlite:///app.db

# Upload
MAX_CONTENT_LENGTH=16777216
UPLOAD_FOLDER=uploads

# Transcrição de Áudio (Whisper) - Opcional
# Para processar arquivos de áudio (MP3, WAV), você precisa:
# 1. Instalar FFmpeg no sistema (obrigatório)
#    - Windows: Baixe de https://ffmpeg.org/download.html e adicione ao PATH
#    - Linux: sudo apt install ffmpeg (Ubuntu/Debian) ou sudo yum install ffmpeg (CentOS/RHEL)
#    - Mac: brew install ffmpeg
# 2. Instalar dependências Python: pip install openai-whisper torch ffmpeg-python
# 3. Na primeira execução, o modelo Whisper será baixado automaticamente (~150MB)
# Modelo Whisper: tiny, base (padrão), small, medium, large
# - tiny: mais rápido, menos preciso
# - base: balance entre velocidade e qualidade (recomendado)
# - large: mais lento, mais preciso
WHISPER_MODEL=base
